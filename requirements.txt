# Vector Database
pymilvus>=2.3.0
qdrant-client>=1.7.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
dask[complete]>=2023.12.0
ray[default]>=2.9.0

# Medical Data Formats
pydicom>=2.4.0
nibabel>=5.2.0
SimpleITK>=2.3.0
biopython>=1.83

# Embeddings & ML (relaxed for compatibility)
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0
open-clip-torch>=2.20.0

# Medical AI Models (relaxed for compatibility)
medclip>=0.0.1
biobert-embedding>=0.1.2

# Storage & Cloud
boto3>=1.34.0
minio>=7.2.0
fsspec>=2023.12.0
s3fs>=2023.12.0

# Orchestration (optional - comment out if not needed)
# apache-airflow>=2.8.0
# prefect>=2.14.0

# API
fastapi>=0.100.0
uvicorn>=0.25.0
pydantic>=2.5.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
requests>=2.31.0
aiohttp>=3.9.0
